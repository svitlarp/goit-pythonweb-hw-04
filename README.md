# goit-pythonweb-hw-04
async python file-sorter

A program that reads source and destination directories from the command line and copies all files from the source directory to the destination directory based on their file extensions.
 

```python3 sorter.py  -s random-files -o sorteddir```

where:
 - **-s**  source directory
 - **-o** output directory

